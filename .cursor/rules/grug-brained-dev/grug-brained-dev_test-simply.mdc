---
description: "Write simple, focused tests that verify behavior"
globs: **/*.test.*, **/*.spec.*, **/test_*
---

---
namespace: grug-brained-dev
ruleset:
  id: grug-brained-dev
  name: Grug Brained Developer Rules
  rules:
    - avoid-abstractions
    - simple-tools
    - avoid-premature-optimization
    - debuggable-code
    - fear-of-looking-dumb
    - readable-over-clever
    - test-simply
    - complexity-enemy
    - say-no
    - eighty-twenty
rule:
  id: test-simply
  name: Test Simply
  enforcement: SHOULD
  priority: 75
  scope:
    - files: ["**/*.test.*", "**/*.spec.*", "**/test_*"]
---

# Test Simply (SHOULD)

grug like tests that tell story

Good test:
- Test one thing
- Clear what it checking
- Easy to understand when fail
- Not too many mocks

Complex test worse than no test
