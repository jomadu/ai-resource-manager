# GitLab Registry Configuration Example
# Copy this file to .env and configure for your GitLab instance

# GitLab instance URL (without protocol, will default to https://)
# Example: gitlab.com or gitlab.example.com
GITLAB_URL=gitlab.example.com

# GitLab project ID (numeric ID found in project settings)
# Example: 12345
GITLAB_PROJECT_ID=

# GitLab Personal Access Token with the following scopes:
# - read_api (to access GitLab API)
# - read_package_registry (to read package registry)
#
# Create a token at: https://<your-gitlab-url>/-/user_settings/personal_access_tokens
GITLAB_TOKEN=

# GitLab API version (default: v4)
GITLAB_API_VERSION=v4

# Optional: Package configuration
# Ruleset name to install (default: ai-rules)
RULESET_NAME=ai-rules

# Optional: Include patterns for filtering package content
# Leave empty to include all YAML files
INCLUDE_PATTERNS=

# Sinks to install to (comma-separated)
SINKS=cursor,q
