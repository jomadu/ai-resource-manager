# Cloudsmith Registry Configuration
# Copy this file to .env and fill in your values

# Cloudsmith API Configuration
CLOUDSMITH_URL=https://api.cloudsmith.io
CLOUDSMITH_OWNER=your-username
CLOUDSMITH_REPOSITORY=your-repository
CLOUDSMITH_TOKEN=your-api-token

# Ruleset Configuration
RULESET_NAME=ai-rules
INCLUDE_PATTERNS=
SINKS=cursor,q

# Instructions:
# 1. Get your Cloudsmith API token from: https://cloudsmith.io/user/settings/api/
# 2. Set CLOUDSMITH_OWNER to your Cloudsmith username or organization
# 3. Set CLOUDSMITH_REPOSITORY to the repository name containing your rulesets
# 4. Set RULESET_NAME to the name of the ruleset you want to test
# 5. Optionally set INCLUDE_PATTERNS to filter specific files (e.g., "*.yml,*.yaml")
# 6. Set SINKS to the output targets (cursor, q, or both)
