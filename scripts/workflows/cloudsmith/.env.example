# Cloudsmith Workflow Configuration
# Copy this file to .env and fill in your values
# OR export CLOUDSMITH_API_KEY in your shell profile (.zshenv, .bashrc, etc.)

# Cloudsmith API Configuration
CLOUDSMITH_URL=https://api.cloudsmith.io
CLOUDSMITH_OWNER=your-username-or-org
CLOUDSMITH_REPOSITORY=your-repository

# Optional: Set token here, or use CLOUDSMITH_API_KEY from your environment
# CLOUDSMITH_TOKEN=your-api-token

# Optional: Test Configuration
RULESET_NAME=ai-rules
INCLUDE_PATTERNS=""
SINKS=cursor,q

# Instructions:
# 1. Get your Cloudsmith API token from: https://cloudsmith.io/user/settings/api/
# 2. Either:
#    a) Export CLOUDSMITH_API_KEY in your shell profile (.zshenv, .bashrc, etc.)
#       export CLOUDSMITH_API_KEY=your-api-token
#    b) Set CLOUDSMITH_TOKEN in this .env file
# 3. Set CLOUDSMITH_OWNER to your Cloudsmith username or organization
# 4. Set CLOUDSMITH_REPOSITORY to the repository name containing your rulesets
# 5. Run: ./init-cloudsmith-sandbox.sh
